web: if [ -d "backend-socialbooster-demo" ]; then cd backend-socialbooster-demo && python manage.py migrate --noinput && gunicorn socialbooster.wsgi:application --bind 0.0.0.0:$PORT; else python manage.py migrate --noinput && gunicorn socialbooster.wsgi:application --bind 0.0.0.0:$PORT; fi
